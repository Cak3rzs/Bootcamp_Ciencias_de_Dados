nome = ("Edy", "Gabriel", "Joao",) # para não dar confusao entre precedencia e tupla no interpretador, usamos uma virgula no final, para indentificar uma tupla

letra = tuple("Python")

numeros = tuple([1,2,3,4,5,6,7])

pais = ("Brasil",)

#É exatamente igual, para acessarmos os valores de uma tupla, a uma lista

nome = [0]
letra = [2]
numeros = [4]
pais = [-1]

#metodos da tupla

nome.count()
letra.index()
len(numeros)